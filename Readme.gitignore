cuda实现CV和fisheye模型的畸变校正 VS2015 + OpenCV3.4.0 + cuda8.0

分为两个.cu文件实现两种模型的畸变校正。

*cudaUndistort.cu：CV模型的畸变校正。其中内参矩阵camK和畸变系数矩阵D，请根据自己的需求读入相应数据。

*cudaFisheyeUndistort.cu：fisheye模型的畸变校正。其中内参矩阵camK和畸变系数矩阵D,请根据自己的需求读入相应数据。

两个.cu文件中的cudaBuildMap相当于opencv中的initUndistortRectifyMap()函数，用来生成坐标映射Mapx和Mapy。
cudaRemap相当于opencv中的remap()函数，用来对输入图像根据mapx和mapy进行像素值的填充。
